version: 2

models:
  - name: dim_users
    description: User name,contact, and address info
    columns:
      - name: user_uuid
        tests:
          - not_null
  - name: fct_orders
    description: order and promo info joined
    columns:
      - name: order_uuid
        tests:
          - not_null       